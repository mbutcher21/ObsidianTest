import * as Models from './models';
import * as Parameters from './parameters';
import { Callback } from '../callback';
import { Client } from '../clients';
import { Pagination } from '../pagination';
export declare class Search {
    private client;
    constructor(client: Client);
    /** Search for entities in Confluence using the Confluence Query Language (CQL) */
    search<T = Pagination<Models.SearchResult>>(parameters: Parameters.SearchContent | undefined, callback: Callback<T>): Promise<void>;
    /** Search for entities in Confluence using the Confluence Query Language (CQL) */
    search<T = Pagination<Models.SearchResult>>(parameters?: Parameters.SearchContent, callback?: never): Promise<T>;
}
